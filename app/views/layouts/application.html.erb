<!DOCTYPE html>
<html>
  <head>
    <title>MacroManager</title>
    <%= csrf_meta_tags %>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>

    <nav class="navbar navbar-default">
      <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <%= link_to "Do The Macros", root_path, class:"navbar-brand" %>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
          <ul class="nav navbar-nav">
            <% if @current_user %>
            <li class=""> <%= link_to "My Macros", macros_path %>
            <li class=""> <%= link_to "Meal Plans", meal_plan_path %>
            <% else %>
            <li class=""> <%= link_to "Macro Calculator", guest_macros_path %>
            <li class=""> <%= link_to "Meal Planner", meal_plan_path %>
            <% end %>
            <li class=""> <%= link_to "About / FAQ", about_path %>
            <li class=""> <%= link_to "Get Coaching", coaching_path %>
          </ul>
          <ul class="nav navbar-nav navbar-right">
            <% if @current_user %>
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Your Account<span class="caret"></span></a>
              <ul class="dropdown-menu">
                <li><%= link_to "My Profile", user_profile_path %></li>
                <li role="separator" class="divider"></li>
                <li><%= link_to "Sign Out", sign_in_path, method: :delete %></li>
              </ul>
            </li>
            <% else %>
                <%= link_to "Sign In", sign_in_path, class: "btn btn-primary navbar-btn btn-spacer" %>
            <% end %>
          </ul>
        </div><!-- /.navbar-collapse -->
      </div><!-- /.container-fluid -->
    </nav>





    <div class="container">
  <% if notice %>
    <div class="alert alert-danger text-center" role="alert">
      <%= notice %>
    </div>
  <% end %>

  <% if alert %>
    <div class="alert alert-danger text-center" role="alert">
      <%= alert %>
    </div>
  <% end %>
</div>

    <%= yield %>
  </body>
</html>
