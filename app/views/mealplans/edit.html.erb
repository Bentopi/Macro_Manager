<div class="container container-edit">
  <br>
  <div class="wow animated fadeIn text-center" data-wow-delay="0.4s">
    <h2 class="mealplan-title"> <%= @mealplan.name %> </h2>
    <hr class="form-divider">
  </div>

  <br>
  <!-- <%= form_for @mealplan, url: add_meal_path do |f| %>
  <div class="form-inline text-center">

    <div class="row">
      <div class="col-xs-offset-0 col-xs-8 col-sm-offset-3 col-sm-3">
        <div class="form-group">
          <%= f.collection_select :meals, Recipe.all, :id, :name, {:include_blank => "Select From Collection (optional)"}, { class:'form-control' } %>
        </div>
      </div>

      <div class="text-center">
          <div class="col-xs-4 col-sm-2">
            <%= f.submit "Add", class:"btn btn-warning btn-block" %>
          </div>
      </div>
    </div>
  <% end %>
  </div> -->

  <div class="row">
      <% @mealplan.meals.each do |meal|%>
        <div class="col-xs-12">
          <strong class="meal-title">
            <%= meal.recipe.name %>
            <small>
              <%= link_to "x", delete_meal_path(@mealplan.id, meal.id ), method: :delete %>
            </small>
          </strong>
          <% meal.recipe.recipeparts.each do |part| %>
          <br>
            <%= pluralize(number_to_human((part.amount * part.ingredient.serving_amount) * meal.quantity), part.ingredient.serving_name) %> <%= part.ingredient.name %>
          <% end %>
          <hr>
        </div>

      <% end %>
    </div>

 <br>
    <div class="row">
      <div class="col-xs-6">
        <%= link_to mealplan_path(id: @mealplan.id) do %>
        <div class="btn btn-success btn-block">
          Go Back
        </div>
        <% end %>
      </div>
      <div class="col-xs-6">
        <%= form_for @mealplan, url: generate_mealplan_path(id: @mealplan.id) do |f| %>
        <div class="form-group">
            <div class="text-center">
                  <%= f.submit "Auto-Complete", class:"btn btn-success btn-block" %>
            </div>
          </div>
        <% end %>
      </div>
    </div>
</div>

  <div class="macro-footer text-center">
    <div class="row text-center">
    <strong>Remaining</strong>
    </div>
    <div class="row">
      <div class="col-xs-3">
          <span class="macro-footer-text"><%= @current_user.calories - @mealplan.kcals %>kcal</span>
      </div>
      <div class="col-xs-3">
          <span class="macro-footer-text">P:
          <%= @current_user.protein - @mealplan.proteins %>g</span>
      </div>
      <div class="col-xs-3">
          <span class="macro-footer-text">C:
          <%= @current_user.carbs - @mealplan.carbs %>g</span>
      </div>
      <div class="col-xs-3">
          <span class="macro-footer-text">F:
          <%= @current_user.fat - @mealplan.fats %>g</span>
      </div>
    </div>
  </div>
